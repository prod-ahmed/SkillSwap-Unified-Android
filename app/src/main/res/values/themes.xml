<resources>
    <!-- Base theme for the app -->
    <style name="Theme.SkillSwap" parent="android:Theme.Material.Light.NoActionBar" />
    
    <!-- Splash Screen Theme -->
    <style name="Theme.SkillSwap.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">#6C63FF</item>
        <item name="postSplashScreenTheme">@style/Theme.SkillSwap</item>
    </style>
</resources>
